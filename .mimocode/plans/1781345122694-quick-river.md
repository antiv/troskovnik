# Plan: UI poboljšanja za detalj računa + kategorije

## Promene

### 1. Smanji padding na detail ekranu
**Fajl:** `lib/features/receipts/presentation/receipt_detail_screen.dart` (linija 131)

Promeniti `EdgeInsets.fromLTRB(16, 16, 16, ...)` → `EdgeInsets.fromLTRB(12, 16, 8, ...)` — manji levi/desni padding.

### 2. Tooltip za naziv artikla
**Fajl:** `lib/features/receipts/presentation/receipt_detail_screen.dart` (linija 347)

Zamenviti `Text(item.name, ...)` sa `Tooltip(message: item.name, ...)`.

### 3. Boja indikatora kategorije
**Fajl:** `lib/features/receipts/presentation/receipt_detail_screen.dart`

Pretvoriti `_ItemTile` iz `StatelessWidget` u `ConsumerWidget`. Kad je `item.categoryId != null`, dovući kategoriju iz `categoriesProvider` i postavi boju ikonice.

### 4. Dodeli SVE stavke jednoj kategoriji
**Fajl:** `lib/features/receipts/presentation/receipt_detail_screen.dart`

Dodati dugme "Categorize all" iznad liste stavki. Klik otvara `CategoryPickerSheet`, i kad korisnik izabere kategoriju — sva `line_items` za taj receipt dobijaju tu kategoriju.

**Implementacija:**
- Novi `CategoryRepository.assignToReceipt(int receiptId, int categoryId)` — bulk update svih line items za receipt
- Dugme u `_DetailBody` iznad sekcije "Stavke"
- Korišćenje `categoryProvider` za osvežavanje UI-a nakon dodele

### 5. Ikonica za upravljanje kategorijama kod Analitike
**Fajl:** `lib/features/analytics/presentation/analytics_screen.dart`

Pored naslova sekcije "Po kategorijama" dodati `IconButton` za navigaciju na `CategoriesScreen`. Ukloniti kategorije iz `settings_screen.dart` (list tile za kategorije).

### 6. Settings ostaje kakav jeste
**Fajl:** `lib/features/settings/settings_screen.dart` — BEZ MENJANJA (zadržati samo jezik)
**Fajl:** `lib/features/home/home_shell.dart` — BEZ MENJANJA (zastavica ostaje)

---

## Kritični fajlovi
- `lib/features/receipts/presentation/receipt_detail_screen.dart` — padding, tooltip, boja kategorije, "categorize all"
- `lib/features/categories/data/category_repository.dart` — nova metoda `assignToReceipt`
- `lib/features/analytics/presentation/analytics_screen.dart` — ikonica za kategorije pored sekcije
- `lib/features/settings/settings_screen.dart` — ukloniti list tile za kategorije

## Verifikacija
1. `fvm flutter analyze` — čisto
2. `fvm flutter test` — svi testovi prolaze
3. Build debug APK + install na emulator
4. Ručno: detalj računa — manji padding, tooltip, obojen indikator, dugme "categorize all"
5. Ručno: analitika — ikonica za kategorije pored sekcije
6. Ručno: settings — samo jezik, bez kategorija
